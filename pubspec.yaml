name: visual_effects_kit
description: Procedural Flutter visual effects for animated backgrounds, surfaces, and interactive UI accents.
version: 0.1.0
topics:
  - flutter
  - animation
  - graphics
  - effects

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
